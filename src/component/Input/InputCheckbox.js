import React from "react";
import ToggleButton from "../ToggleButton";

const InputCheckbox = ({ className, children }) => {
  return (
    <div>
      <ToggleButton />
    </div>
  );
};

export default InputCheckbox;
